#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"

export PI_CODING_AGENT_DIR="$REPO_ROOT/agent"
export PI_CONFIG_DIR=".omp"

exec ~/.bun/bin/bun --cwd="$REPO_ROOT" run dev "$@"
